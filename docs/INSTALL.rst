idwfed.theme Installation
-------------------------

To install idwfed.theme using zc.buildout and the plone.recipe.zope2instance
recipe to manage your project, you can do this:

* Add ``idwfed.theme`` to the list of eggs to install, e.g.:

    [buildout]
    ...
    eggs =
        ...
        idwfed.theme

* Re-run buildout, e.g. with:

    $ ./bin/buildout

